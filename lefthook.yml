pre-commit:
  parallel: true
  commands:
    lint:
      glob: "**/*.{js,ts,jsx,tsx}"
      run: pnpm eslint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "**/*.{js,ts,jsx,tsx,json,css,md}"
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
